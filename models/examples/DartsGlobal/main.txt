"""
{model_name} using darts.

Performs global forecasting using a {model_name}. Reports a single global MASE.
"""
from pathlib import Path
from warnings import filterwarnings
filterwarnings("ignore") 
import os

{imports}

import sys
sys.path.append('../')
from env_reader import env_reader
from model_classes.darts_global_class import DartsGlobal

if __name__ == "__main__":
    
    env_vars = env_reader(os.environ)

    {obj_name}_obj = DartsGlobal({estimator_func},
                            "{obj_name}",
                            *env_vars,
                            scaling = True,
                            interpolation=True,
                            f32 = True)
    
    {obj_name}_obj.main_workflow()